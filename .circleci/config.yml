version: 2.1

orbs:
  node: circleci/node@5
  ruby: circleci/ruby@2

jobs:
  build:
    docker:
      - image: cimg/ruby:3.2-node
    steps:
      - checkout

      # Add SSH key
      - add_ssh_keys:
          fingerprints:
            - "XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX" # Replace with your SSH key fingerprint

      # Install Ruby dependencies
      - ruby/install-deps

      # Install Node dependencies
      - node/install-packages:
          pkg-manager: npm

      # Store both Ruby and Node dependencies in cache
      - save_cache:
          key: v1-deps-{{ .Branch }}-{{ checksum "Gemfile.lock" }}-{{ checksum "package-lock.json" }}
          paths:
            - vendor/bundle
            - node_modules

      # Run deploy script
      - run:
          name: Deploy
          command: yarn run deploy

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
